library dart._internal;

// These parts are in tool/input_sdk/internal/, but they are copied by the
// patch_sdk.sh script into the gen/patched_sdk/lib/internal/ directory, so the
// part declarations refer to them as if they are in the same directory as this
// file.
part 'java_annotations.dart';
part 'java_int.dart';
part 'java_string.dart';
